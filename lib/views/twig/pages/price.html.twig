{% extends "twig/layouts/base.html.twig" %}

{% block PRICE %}

<div class="container">
	<div class="box">
		<div class="content">
	<h1>Spot price of
		{{ criptoCurrencyTAG }}
		is
		{{ pairValue }}
		{{ currencyTAG }}</h1>
	{{ post.published_at|date("d/m/Y") }}
	{{ post.published_at|date("H:i:s") }}
			</div>
	</div>
</div>

{% endblock %}
